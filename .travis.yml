language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-percolator' ]; then cd .. && eval "mv $currentfolder generator-percolator" && cd generator-percolator; fi

